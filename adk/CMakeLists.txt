file(GLOB HEADERS RELATIVE ${CMAKE_CURRENT_SOURCE_DIR} *.h)
file(GLOB SOURCES RELATIVE ${CMAKE_CURRENT_SOURCE_DIR} *.c)
add_library(adk SHARED ${HEADERS} ${SOURCES})
install(TARGETS adk LIBRARY DESTINATION ${INSTALL_LIB_DIR})
